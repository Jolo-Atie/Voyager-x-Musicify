export function getAdvisory(info, wind, maxWind, rainChance) { if (info.category === "storm" || maxWind >= 60 || rainChance >= 80) return { level:"danger", emoji:"🔴", label:"Travel not recommended", text:"Severe weather expected — travel not recommended." }; if (["rain","snow","fog"].includes(info.category) || maxWind >= 35 || rainChance >= 45 || wind >= 35) return { level:"caution", emoji:"🟡", label:"Travel with caution", text:"Changeable conditions — travel with caution." }; return { level:"safe", emoji:"🟢", label:"Safe to travel", text:"Conditions look clear — safe to travel." }; }
